<Page
    x:Class="BiliSubStudio.App.Pages.SubtitlePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    AutomationProperties.Name="Phụ đề Bilibili">
    <ScrollViewer><Grid x:Name="PageRoot" Padding="28" MaxWidth="1100" HorizontalAlignment="Left">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel Spacing="5"><TextBlock Style="{StaticResource PageTitleStyle}" Text="Phụ đề Bilibili"/><TextBlock Style="{StaticResource BodyTextStyle}" Text="Lấy track chính thức hoặc AI và xuất SRT, TXT hay JSON."/></StackPanel>
        <Border Grid.Row="1" Margin="0,22,0,0" Style="{StaticResource CardBorderStyle}"><StackPanel Spacing="14">
            <Grid ColumnSpacing="10"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox x:Name="UrlBox" Style="{StaticResource InputTextBoxStyle}" PlaceholderText="URL video Bilibili" TextChanged="UrlBox_TextChanged"/><Button x:Name="LoadButton" Grid.Column="1" AccessKey="T" Click="Load_Click" Content="Lấy track"/></Grid>
            <TextBlock x:Name="TitleText" Style="{StaticResource MutedTextStyle}" Text="Chưa lấy danh sách track."/>
            <Grid x:Name="TrackGrid" ColumnSpacing="12" RowSpacing="10"><Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="0"/></Grid.RowDefinitions><StackPanel><TextBlock Style="{StaticResource MutedTextStyle}" Text="Track"/><ComboBox x:Name="TrackBox" DisplayMemberPath="DisplayName"/></StackPanel><StackPanel x:Name="FormatPanel" Grid.Column="1"><TextBlock Style="{StaticResource MutedTextStyle}" Text="Định dạng"/><ComboBox x:Name="FormatBox" SelectedIndex="0"><x:String>srt</x:String><x:String>txt</x:String><x:String>json</x:String></ComboBox></StackPanel></Grid>
            <StackPanel Orientation="Horizontal" Spacing="10"><Button x:Name="StartButton" Click="Start_Click" Content="Tải phụ đề" IsEnabled="False"/><Button x:Name="CancelButton" Click="Cancel_Click" Content="Hủy" IsEnabled="False" Style="{StaticResource DangerButtonStyle}"/></StackPanel>
        </StackPanel></Border>
        <Border Grid.Row="2" Margin="0,16,0,0" Style="{StaticResource CardBorderStyle}"><StackPanel Spacing="10"><ProgressBar x:Name="Progress" Height="8" AutomationProperties.Name="Tiến độ tải phụ đề"/><TextBlock x:Name="StatusText" Style="{StaticResource BodyTextStyle}" Text="Sẵn sàng." AutomationProperties.LiveSetting="Polite"/><TextBox x:Name="LogBox" Height="160" IsReadOnly="True" AcceptsReturn="True" FontFamily="Cascadia Mono" TextWrapping="Wrap" AutomationProperties.Name="Nhật ký tải phụ đề"/></StackPanel></Border>
    </Grid></ScrollViewer>
</Page>
